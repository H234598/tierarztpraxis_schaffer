{
  "name": "tierarztpraxis-schaffer-contact-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "check": "wrangler types --check && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit && tsc -p test-runtime/tsconfig.json --noEmit",
    "test": "vitest run && vitest run --config vitest.workers.config.ts",
    "d1:migrate:local": "wrangler d1 migrations apply TRANSFER_DB --local --env development",
    "d1:migrate:development": "wrangler d1 migrations apply TRANSFER_DB --remote --env development",
    "dev": "wrangler dev --remote --env development",
    "deploy:development": "wrangler deploy --env development",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "@types/node": "24.13.3",
    "typescript": "5.8.3",
    "vitest": "4.1.10",
    "wrangler": "4.118.0"
  },
  "dependencies": {
    "jose": "^6.2.7"
  }
}
