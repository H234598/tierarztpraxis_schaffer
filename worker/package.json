{
  "name": "tierarztpraxis-schaffer-contact-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev --remote --env development",
    "deploy:development": "wrangler deploy --env development",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vitest": "4.1.10",
    "wrangler": "4.107.0"
  }
}
